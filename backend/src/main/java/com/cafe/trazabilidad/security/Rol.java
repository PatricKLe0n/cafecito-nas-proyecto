package com.cafe.trazabilidad.security;

/**
 * Roles de autorización soportados por la aplicación.
 * {@code ADMIN} concede acceso completo (lectura y escritura); {@code USER}
 * concede acceso de solo lectura sobre los recursos protegidos.
 */
public enum Rol { ADMIN, USER }
